# config.yaml

app_name: MyApp
version: 1.0.0
debug: true

database:
  host: localhost
  port: 1234
  username: admin
  password: secret777

features:
  enable_logging: true
  enable_notifications: false