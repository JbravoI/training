# config.yaml

app_name: MyApp
version: 1.0.0
debug: true

database:
  host: localhost
  port: 5432
  username: admin
  password: secret

features:
  enable_logging: true
  enable_notifications: false